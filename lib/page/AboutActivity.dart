import 'package:flutter/material.dart';

class AboutActivity extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text("Ini About"),
        ),
      ),
    );
  }
}
